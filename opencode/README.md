# opencode config

Este README foi incorporado no README global do checkout.

Consulte:

```text
README.md
```

Este diretório contém:

- `agents/` — agentes do OpenCode.
- `skills/` — skills do OpenCode.
- `plugins/` — plugins locais do OpenCode.
- `opencode.json` — configuração global versionada.
